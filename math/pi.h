#ifndef MATH_PI_H
#define MATH_PI_H

#include <math/real.h>

namespace math {
 
  const real pi = M_PI;
  const real half_pi = M_PI / 2.0;

  const real rad = 1;
  const real deg = pi / 180.0;

}

#endif
