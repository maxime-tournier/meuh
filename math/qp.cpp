#include "qp.h"

#include <math/error.h>

namespace math {


  // qp::qp(const mat& Q, const mat& A)
  //   : Q(Q), A(A) { 

  //   if( Q.rows() != Q.cols() ) throw error("Q must be square");
  //   if( A.cols() != Q.cols() ) throw error("A and Q must have the same width");
    
  //   // TODO assert A.rows() <= Q.rows() sinon on est mal
  // }

  // qp::qp(mat&& Q, mat&& A)
  //   : Q(std::move(Q)), 
  //     A(std::move(A)) { 
    
  //   if( Q.rows() != Q.cols() ) throw error("Q must be square");
  //   if( A.cols() != Q.cols() ) throw error("A and Q must have the same width");
    
  //   // TODO assert A.rows() <= Q.rows() sinon on est mal
  // }

  // qp::qp(qp&& other) 
  // : Q(std::move(other.Q)),
  //   A(std::move(other.A)) {

  // }
  






  
  
}
